sigildb-data:
  image: busybox
  command: /bin/true
  volumes: 
    - /var/lib/postgresql/data
  
harubadb-data:
  image: busybox
  command: /bin/true
  volumes: 
    - /var/lib/postgresql/data

sigildb:
  image: postgres
  volumes_from:
    - sigildb-data

harubadb:
  image: postgres
  volumes_from: 
    - harubadb-data
  
nginx:
  image: nginx
  volumes:
   - ./nginx.conf:/etc/nginx/conf.d/default.conf
   - .:/sigil
  volumes_from:
   - sigil
   - haruba
  ports:
   - "80:80"
  links:
   - sigil
   - haruba
  
sigil:
  build: .
  environment:
    - DEBUG=yes
    - HOST=0.0.0.0
    - SQLALCHEMY_DATABASE_URI=postgres://postgres@sigildb
    - ENABLE_2FA=no
    - TOTP_DOMAIN=docker.dev
    - API_URL_PREFIX=/sigil-api
    - SESSION_TOKEN_MAX_AGE=300
    - MAIL_SUPPRESS_SEND=no
    - MAIL_SERVER=smtp-adimian.alwaysdata.net
    - MAIL_USERNAME=sigil@adimian.com
    - MAIL_PORT=587
    - MAIL_USE_TLS=yes
    - MAIL_PASSWORD=aQjRk8wDhqS5R50DrDO9
    - MAIL_DEFAULT_SENDER=sigil@adimian.com
    - UI_BASE_URL=http://docker.dev/sigil
  volumes:
    - .:/sigil
  links:
    - sigildb

haruba:
  image: registry.adimian.com/haruba/haruba
  environment:
    - SQLALCHEMY_DATABASE_URI=postgres://postgres@harubadb
    - API_URL_PREFIX=/haruba-api
    - SIGIL_API_URL=http://sigil:5000/sigil-api
    - SIGIL_APP_KEY=WzM0LCJmZjIzOWM1NGQwZjMwZTQ0NjdmZjRmMzdjOTZjZGZkMSJd.CMs2PA.6EKaxzLHpPnmu8aza-BCEjmJS7A
  links:
    - harubadb
    - sigil
  